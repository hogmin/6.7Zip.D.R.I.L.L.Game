/**
 * Властивості компонента картки гри сайту "d.r.i.l.l.".
 * * @remarks
 * Описують дані, які потрібні для відображення однієї гри в переліку проєктів.
 */
export interface GameCardProps {
  /** Назва гри. */
  title: string;
  /** Короткий опис гри або її особливостей. */
  description: string;
  /** Маршрут до детальної сторінки гри. */
  link: string;
  /** Необов`язковий шлях до зображення гри. */
  imageSrc?: string;
}

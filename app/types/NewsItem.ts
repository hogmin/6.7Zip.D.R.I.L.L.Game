/**
 * Тип окремої новини сайту "d.r.i.l.l.".
 * * @remarks
 * Використовується для зберігання новин у контексті та для їх відображення в інтерфейсі.
 */
export type NewsItem = {
  /** Унікальний ідентифікатор новини. */
  id: number;
  /** Дата або період публікації. */
  date: string;
  /** Заголовок новини. */
  title: string;
  /** Опис або основний текст новини. */
  description: string;
};

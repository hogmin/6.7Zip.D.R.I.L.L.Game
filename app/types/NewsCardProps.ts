/**
 * Властивості компонента картки новини сайту "d.r.i.l.l.".
 * * @remarks
 * Містять дані однієї новини та callback для її видалення.
 */
export interface NewsCardProps {
  /** Унікальний ідентифікатор новини. */
  id: number;
  /** Дата або часовий маркер публікації. */
  date: string;
  /** Заголовок новини. */
  title: string;
  /** Короткий текстовий опис новини. */
  description: string;
  /** Функція видалення новини за її ідентифікатором. */
  onDelete: (id: number) => void;
}
